name: Build and Push Docker Image

on:
  push:
    branches:
      - ray6811-patch-1
  pull_request:
    branches:
      - ray6811-patch-1

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Build the React app Docker image
        run: docker build -t ray6811/bookstore:latest .

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ ray6811 }}
          password: ${{ N7IP1o7Ji_f9I4y6l7tXHSJ8YlQ }}

      - name: Push Docker image to Docker Hub
        run: docker push ray6811/bookstore:latest

      # (Optional) Upload build artifacts if you still want to:
      # - name: Upload build artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: react-build
      #     path: build/
